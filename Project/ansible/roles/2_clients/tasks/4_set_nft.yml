---
- name: Validate condition result
  debug:
     msg: "The condition result is {{ var_group }}"

- name: Развернуть nftables.conf
  template:
    src: "template/nft/nftables_cl.conf.j2"
    dest: /etc/nftables.conf
    owner: root
    group: root
    mode: '0600'
  notify:
    - restart_nft
