global:
  scrape_interval: {{ prometheus_scrape_interval }}

scrape_configs:
  - job_name: 'node'
    static_configs:
      - targets:
        {% for host in groups['internal_zone'] %}
        - '{{ hostvars[host].ansible_host }}:9100'
        {% endfor %}
