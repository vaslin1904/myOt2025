<?php
// Конфигурация WordPress + CRUD
define('DB_NAME', 'wordpress');
define('DB_USER', 'wpuser');
define('DB_PASSWORD', '{{ mysql_wp_password }}');
define('DB_HOST', '10.10.1.51');
define('DB_CHARSET', 'utf8mb4');
define('DB_COLLATE', '');

// Путь к админке
define('ADMIN_PATH', __DIR__ . '/admin');
define('ADMIN_URL', 'http://localhost/admin/');
// Добавляем функцию для получения подключения к БД
function get_db_connection() {
    static $conn = null;
    if ($conn === null) {
        $conn = new mysqli(DB_HOST, DB_USER, DB_PASSWORD, DB_NAME);
        if ($conn->connect_error) {
            die("Ошибка подключения к БД: " . $conn->connect_error);
        }
    }
    return $conn;
}
require_once ABSPATH . 'wp-settings.php';
